# Local (stdio) use only — connection-from-request mode ignores these and reads
# the target from the X-Kubero-DB-URI / X-Kubero-Access-Mode request headers.
S3_ENDPOINT_URL=http://localhost:9000
AWS_ACCESS_KEY_ID=rustfsadmin
AWS_SECRET_ACCESS_KEY=changeme
