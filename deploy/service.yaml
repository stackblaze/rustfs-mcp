apiVersion: v1
kind: Service
metadata:
  name: rustfs-mcp
  namespace: kubero-mcp
  labels:
    app.kubernetes.io/name: rustfs-mcp
spec:
  selector:
    app.kubernetes.io/name: rustfs-mcp
  ports:
  - name: http
    port: 3010
    targetPort: http
